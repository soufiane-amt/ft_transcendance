
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model User {
  id          String @id @default(uuid())
  login       String
  firstname   String
  lastname   String
  fullname   String
  email       String @unique
  avatar      String
}